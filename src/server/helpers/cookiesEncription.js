const cookieEncrypt = function(identification) {
  return identification;
};

const cookieDecrypt = function(identification) {
  return identification;
};

module.exports = { cookieEncrypt, cookieDecrypt };
